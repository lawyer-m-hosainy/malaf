# سوبر برومبت: الوصول بمنصة مَلَف إلى 100/100

## التعليمات — انسخ البرومبت التالي وأرسله كما هو:

---

```
You are a senior full-stack engineer specializing in React 19 + Supabase + Node.js.
Your mission: bring Malaf (Egyptian law firm SaaS) from 78/100 to 100/100.

CURRENT STATE (already working — DO NOT break):
- 28 React pages with Arabic RTL UI ✅
- 0 TypeScript errors (tsc --noEmit passes) ✅
- AES-256-GCM encryption with tenant key derivation ✅
- JWT verification (HS256) via middleware/auth.js ✅
- Helmet.js + CSP + Rate Limiting ✅
- 16 Zustand stores with reset() on logout ✅
- legalDataService.ts: CRUD for clients, cases, sessions, invoices, tasks, enforcement, expenses, trust_accounts, eta_invoices, conflict_checks, wiki_articles ✅
- WhatsApp Bot backend complete (routes/whatsapp.js 554 lines + 4 services) ✅
- render.yaml configured for Render.com (frankfurt region) ✅

═══════════════════════════════════════════════════════════
GAP 1: Connect 13 UI-only pages to Supabase (DB Score: 72→100)
═══════════════════════════════════════════════════════════

These 13 pages use local useState/Zustand only. Connect them to Supabase
via legalDataService.ts using the SAME patterns (requireOrgId, org_id filter,
soft delete with deleted_at, error handling).

Pages to connect (each needs fetch + save + delete in legalDataService.ts):

1. Documents.tsx (src/views/Documents.tsx)
   → Table: documents (id, org_id, name, case_id, type, size_bytes, storage_path, uploaded_by, created_at, deleted_at)
   → Use Supabase Storage bucket "documents" for actual file upload/download
   → Replace hardcoded demo data on lines 69-74 with fetchDocuments()

2. TimeTracking.tsx (src/views/TimeTracking.tsx)
   → Table: time_entries (id, org_id, case_id, lawyer_id, description, duration_minutes, date, billable, is_billed, created_at, deleted_at)
   → Currently uses useFinanceStore.timeEntries (in-memory only)
   → Keep Zustand as cache but sync to Supabase on add/update/delete

3. Collections.tsx (src/views/Collections.tsx)
   → Table: receivables (id, org_id, client_id, client_name, case_id, total_amount, collected_amount, outstanding_amount, due_date, status, is_reconciled, created_at, deleted_at)
   → Table: collection_actions (id, org_id, receivable_id, type, notes, created_by, created_at)
   → Currently uses useFinanceStore.receivables (in-memory only)

4. ExpertMissions.tsx (src/views/ExpertMissions.tsx)
   → Table: expert_missions (id, org_id, case_id, mission_number, expert_name, expert_type, assignment_date, deposit_amount, report_received, objection_filed, status, created_at, deleted_at)
   → Table: expert_sessions (id, mission_id, date, result, next_session, created_at)
   → Currently uses useExpertStore.missions (in-memory only)

5. SpecializedTracks.tsx — already uses saveSpecializedCase/fetchSpecializedCases ⚠️ verify it works

6. ELitigation.tsx (src/views/ELitigation.tsx)
   → Table: e_litigation_cases (id, org_id, case_id, portal_ref, portal_status, last_sync, created_at)

7. StateCouncil.tsx → already has state_council_cases table in migration 009 ✅
   → Connect via fetchSpecializedCases('state_council_cases')

8. EconomicCourt.tsx → already has economic_court_cases table in migration 009 ✅
   → Connect via fetchSpecializedCases('economic_court_cases')

9. CriminalCases.tsx → already has criminal_cases table in migration 009 ✅
   → Connect via fetchSpecializedCases('criminal_cases')

10. FamilyCourts.tsx → already has family_court_cases table in migration 009 ✅
    → Connect via fetchSpecializedCases('family_court_cases')

11. BarAssociation.tsx → already has bar_association_services table in migration 009 ✅
    → Connect via fetchSpecializedCases('bar_association_services')

12. FieldCheckins.tsx (src/views/FieldCheckins.tsx)
    → Table: field_checkins (id, org_id, lawyer_id, case_id, location_lat, location_lng, location_name, photo_url, notes, checkin_time, created_at)

13. Contracts.tsx (src/views/Contracts.tsx)
    → Table: contracts (id, org_id, client_id, title, type, content, status, signed_at, created_at, deleted_at)

DELIVERABLE: 
- Write a single SQL migration file: migrations/011_remaining_tables.sql
  Include: CREATE TABLE IF NOT EXISTS, RLS, indexes on org_id, triggers
- Add fetch/save/delete functions to src/services/legalDataService.ts
- Update each page to use the new functions (replace useState demo data)
- Do NOT change any existing working functionality

═══════════════════════════════════════════════════════════
GAP 2: Supabase Storage for Documents (DB Score boost)
═══════════════════════════════════════════════════════════

Currently Documents.tsx fakes file upload. Implement real Supabase Storage:

1. Create storage bucket "documents" with per-org folder isolation:
   Policy: INSERT/SELECT/DELETE where (bucket_id = 'documents') AND (storage.foldername(name))[1] = (auth.jwt() -> 'user_metadata' ->> 'org_id')

2. In legalDataService.ts add:
   - uploadDocument(file: File, caseId: string): Promise<{path: string, url: string}>
   - downloadDocument(path: string): Promise<Blob>
   - deleteDocument(path: string): Promise<void>
   Upload path format: {org_id}/{case_id}/{timestamp}_{filename}

3. Update Documents.tsx to use real upload/download

═══════════════════════════════════════════════════════════
GAP 3: Add Zod validation to routes/ai.js (Security: 85→95)
═══════════════════════════════════════════════════════════

File: routes/ai.js (7KB)
Problem: No input validation on AI endpoints.
Fix: Add Zod schemas matching the pattern in routes/crypto.js:

const analyzeSchema = z.object({
  prompt: z.string().min(1).max(10000),
  type: z.enum(['legal_analysis', 'contract_review', 'case_summary', 'general']).optional(),
});

const documentSchema = z.object({
  content: z.string().min(1).max(50000),
  documentType: z.string().optional(),
});

Apply to all POST endpoints in ai.js. Return 400 with Arabic error on validation failure.

═══════════════════════════════════════════════════════════
GAP 4: Extract duplicate WhatsApp sender to shared helper (Backend: 80→90)
═══════════════════════════════════════════════════════════

In server.js lines 250-276 and 284-312, the WhatsApp message sending
logic is duplicated (initSubscriptionCron and initScheduler both have
identical fetch logic).

Fix:
1. Create services/whatsapp/messageSender.js:
   export async function sendWhatsAppMessage(phone, message, orgId) { ... }
2. Use it in both initSubscriptionCron and initScheduler callbacks
3. Also use it in routes/whatsapp.js POST /send endpoint

═══════════════════════════════════════════════════════════
GAP 5: CSV Import for Clients & Cases (Feature completeness)
═══════════════════════════════════════════════════════════

Investors WILL ask about data migration. Add basic CSV import:

1. In Clients.tsx: Add "استيراد CSV" button next to "إضافة موكل"
2. Parse CSV with columns: name, type, phone, email, national_id
3. Validate each row with Zod (clientSchema from src/lib/schemas.ts)
4. Bulk insert via saveClient() for each valid row
5. Show results: X imported, Y skipped (with reasons)
6. Same pattern for Cases.tsx with: plaintiff, defendant, court, case_number, case_year

No external library needed — use FileReader + split(',') + basic parsing.

═══════════════════════════════════════════════════════════
GAP 6: npm audit fix (Security: 95→100)
═══════════════════════════════════════════════════════════

Run: npm audit --production
Fix ONLY critical and high severity vulnerabilities.
Do NOT change any working code or break existing dependencies.

═══════════════════════════════════════════════════════════
GAP 7: PWA Manifest + Service Worker (Mobile readiness)
═══════════════════════════════════════════════════════════

Add Progressive Web App support so it installs on phones:

1. Create public/manifest.json:
   {
     "name": "مَلَف — إدارة مكتب المحاماة",
     "short_name": "مَلَف",
     "start_url": "/dashboard",
     "display": "standalone",
     "background_color": "#0f172a",
     "theme_color": "#6366f1",
     "dir": "rtl",
     "lang": "ar",
     "icons": [{ "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" }, { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }]
   }

2. Add to index.html: <link rel="manifest" href="/manifest.json">
3. Register basic service worker for offline shell caching

═══════════════════════════════════════════════════════════
GAP 8: Supabase Realtime for Dashboard (Performance: 80→90)
═══════════════════════════════════════════════════════════

Dashboard.tsx currently loads data on mount only.
Add Supabase Realtime subscription for sessions table:

useEffect(() => {
  const channel = supabase
    .channel('sessions-changes')
    .on('postgres_changes', { event: '*', schema: 'public', table: 'sessions', filter: `org_id=eq.${orgId}` }, (payload) => {
      // Refresh sessions data
    })
    .subscribe();
  return () => { supabase.removeChannel(channel); };
}, [orgId]);

This gives live updates when another team member adds a session.

═══════════════════════════════════════════════════════════
RULES — READ CAREFULLY:
═══════════════════════════════════════════════════════════

1. Do NOT modify: middleware/auth.js, routes/crypto.js, src/lib/encryption.ts,
   src/components/AuthProvider.tsx — these are security-critical and working
2. Do NOT change ENCRYPTION_KEY or any encryption logic
3. Do NOT remove any existing RLS policies
4. All new tables MUST have: org_id UUID REFERENCES organizations(id), RLS enabled,
   index on org_id, tenant_isolation policy using get_user_org_id()
5. All new Supabase queries MUST use requireOrgId() and .eq('org_id', orgId)
6. All Arabic text must be correct Egyptian legal terminology
7. After ALL changes: run `npx tsc --noEmit` — ZERO errors required
8. Write SQL migration as ONE file (idempotent, safe to re-run with IF NOT EXISTS)
9. Do NOT install new npm packages unless absolutely necessary

PRIORITY ORDER: GAP 1 → GAP 3 → GAP 4 → GAP 2 → GAP 5 → GAP 7 → GAP 8 → GAP 6

Write your full response in Arabic.
Start with GAP 1 (the biggest impact).
```

---

## ملاحظات للاستخدام:

### قبل إرسال البرومبت:
1. **نفّذ أولاً** المهاجرات المعلّقة في Supabase SQL Editor:
   - `migrations/008_whatsapp_tables.sql`
   - `migrations/009_r1_completeness.sql`  
   - `migrations/R8_sessions_org_id.sql`
2. **أضف** `SUPABASE_JWT_SECRET` في `.env.local`
3. **أضف** `GEMINI_API_KEY` في `.env.local` و Render

### بعد تنفيذ البرومبت:
- شغّل `npx tsc --noEmit` للتأكد من صفر أخطاء
- شغّل `npm run build` للتأكد من نجاح البناء
- شغّل `npm audit --production` للتأكد من الأمان
- اختبر تسجيل الدخول + الخروج للتأكد من مسح البيانات
- ارفع التحديثات: `git add . ; git commit -m "100/100 completion" ; git push origin main`
